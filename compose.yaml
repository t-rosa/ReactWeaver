services:
  # react-weaver.preview:
  #   image: react-weaver-server:latest
  #   restart: always
  #   ports:
  #     - 127.0.0.1:3000:8080
  #   environment:
  #     - ConnectionStrings:Default=Host=react-weaver.db;Port=5432;Database=react-weaver;Username=postgres;Password=postgres
  #   depends_on:
  #     - react-weaver.db
  #   volumes:
  #     - ./ReactWeaver.Server/keys/storage:/app/keys/storage

  react-weaver.db:
    image: postgres:18.1
    restart: always
    user: postgres
    volumes:
      - react-weaver.db-data:/var/lib/postgresql
    environment:
      - POSTGRES_PASSWORD=postgres
    ports:
      - 5432:5432
    expose:
      - 5432
    healthcheck:
      test: ["CMD", "pg_isready"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  react-weaver.db-data:
